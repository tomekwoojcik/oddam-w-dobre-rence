/* eslint-disable react/prop-types */
import React from 'react'

const WhoWeHelpButton = ({ titleButton }) => {
  return (
      <form className='whoWeHelpButton'><button className='button'>{titleButton}</button></form>
  )
}

export default WhoWeHelpButton
