import React from 'react'

// eslint-disable-next-line react/prop-types
const FirstPartText = ({ textHeader }) => {
  return (
    <h3 className='textInHeader'>{textHeader}</h3>
  )
}

export default FirstPartText
